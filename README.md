# tp122 时区与时间换算工作台

一个本地小工具：把各地时区连同夏令时规则登记进来，再把某一个时刻换算到所有已登记的时区，页面上直接看出各地是当地的几点、谁的日期已经翻到前一天或者后一天、跟来源时区差了几个小时。另外单开一块时差对照：任选两条档案，给出两地的标准偏移差、此刻是否有一边正处在夏令时生效期内、夏令时生效期内两侧时差分别变成多少、一年里时差变化的时段，以及两地当地日期最多可能差几天、翻到前后一天时边界落在当地几点。时区名写得不合规矩、偏移超出范围、夏令时偏移不比标准偏移靠前、开始与结束两段规则写得一模一样、结束年份早于开始年份，保存时当场就会指出来。

数据全部存在本机的数据文件里，不连数据库，也不调用任何外部服务。

## 技术栈

- 后端：Node.js（>=18）与 Express，前后端同一个进程
- 前端：原生 HTML、CSS 与 JavaScript，页面文件由后端静态目录直接提供，没有构建步骤
- 存储：本地 JSON 文件 `data/db.json`

## 项目结构

```
tp122/
├── server/
│   ├── index.js        # 服务入口、静态目录、接口路由
│   ├── api.js          # 时区档案、换算与对照的对外动作
│   ├── zones.js        # 档案的校验、新增、修改、查询与删除
│   ├── convert.js      # 按登记偏移把一个时刻换算到各时区
│   ├── compare.js      # 两条档案的时差、夏令时时段与翻日边界
│   ├── errors.js       # 带错误码与出错位置的业务异常
│   └── store.js        # 数据读写：载入、整理结构与落盘
├── public/
│   ├── index.html      # 主界面：时区档案区与换算台
│   ├── app.js          # 页面交互逻辑
│   └── style.css       # 页面样式
├── data/
│   └── db.json         # 本地数据文件（含十条时区档案）
├── package.json
├── .gitignore
└── README.md
```

## 启动方式

```bash
npm install
npm start
```

启动后打开页面地址：<http://localhost:5122>

端口固定为 `5122`，如需临时改动可在启动前设置环境变量 `PORT`。

## 页面怎么用

1. 时区档案区列出时区名称、显示名称、标准偏移、是否实行夏令时、夏令时偏移、切换规则、生效年份与备注；可以按是否实行夏令时筛选，也能按名称、显示名或备注搜索
2. 点新建档案填上时区名与偏移；实行夏令时的档案还要把夏令时偏移与开始、结束两段切换规则（第几个星期几的几点几分）以及生效年份填上
3. 换算台选好日期、时刻与来源时区，点换算一遍，页面列出每个已登记时区的当地日期、当地时刻、星期、与来源是否同一天、偏移以及跟来源时区差多少
4. 时差对照选好甲、乙两条档案与年份（年份不填按今年），点对照一遍：先给标准偏移差与此刻两边的夏令时状态和实际时差，再列甲标准或夏令时、乙标准或夏令时各种组合下的时差（并标出选定年份是否真的会出现），然后按时间顺序列出这一年时差变化的时段与每次切换，最后给出当地日期最多相差几天以及翻到前一天、后一天时两地各自是当地几点
5. 任何一步不成立时，页面顶部给出说明，并尽量把问题标到对应的输入项上

## 换算口径

- 换算按登记的标准偏移进行：先把输入的当地时刻减去来源时区的标准偏移得到基准时刻，再逐个时区加上各自的偏移
- 每个时区的当地日期与时刻都由基准时刻加上该时区的偏移算出，日期是否翻到前一天或后一天按公历日比较
- 与来源时区的时差按两边偏移的差值给，整小时只写小时，带分钟的把分钟也写出来
- 结果按偏移从小到大排列，来源时区那一条在页面上会标出来
- 档案里登记了夏令时规则不等于换算时已经计入夏令时，规则本身如何落到具体日期上需要另行处理

## 时差对照口径

- 标准偏移差按乙地标准偏移减甲地标准偏移给，正数表示乙地比甲地靠前，整小时只写小时，带分钟的把分钟也写出来；同一条档案与自己对照直接给零时差的结论
- 夏令时切换的具体时刻由"第几个星期几几点几分"推算：开始段上的时分按标准时钟面折算，结束段上的时分按夏令时钟面折算（例如纽约结束写两点，指快一小时的两点拨回一点）；开始月份晚于结束月份视为南半球跨年
- 某一年早于开始年份或晚于结束年份时，该档案这一年全年按标准偏移；此刻的夏令时状态按服务端当前时间单独判定
- 甲、乙各取标准时与夏令时，列出四种组合下的时差，并标出选定年份里哪些组合真的会同时出现；再按两侧全部切换时刻把这一年切成若干时段，逐段写明实际时差
- 两条档案标准偏移相同、恰好一边实行夏令时而另一边不实行时，单独写清夏令时生效的起止时段里时差变成多少（通常是一小时），其余时段零时差；生效年份不覆盖所选年份时说明这一年全年零时差
- 当地日期最多相差几天只按选定年份里实际同时出现过的偏移组合算：偏移差为 D 分钟时日期差在 floor(D/1440) 与 ceil(D/1440) 之间，跨度上限取后者（例如 UTC-12 对 UTC+14 差 1560 分钟，最多差两天）
- 翻日边界按钟面写清：靠前一侧当地 00:00 翻到新一天时另一侧还是前一天的几点，另一侧翻到 00:00 时这一侧又已经是几点；偏移差恰好是整天时说明两地永远相差同样的天数

## 登记口径

- 时区名称写成地区加城市，例如 `Asia/Shanghai`；基准时可以写 `UTC`；最长 40 个字符，不能重复
- 标准偏移与夏令时偏移都按整数分钟登记，范围是 -720 到 840，带半小时或三刻的地区直接写对应的分钟数（例如东五区半写 330、加德满都写 345）
- 实行夏令时的档案必须填夏令时偏移，且要比标准偏移更大（数值上更靠前）
- 切换规则分开始与结束两段，每段写清月份、第几个星期（一到四或者最后一个）、星期几、小时与分钟
- 生效年份填 1900 到 2100 之间；结束年份留空表示至今有效，已经停止实行夏令时的地区要把结束年份填上
- 备注用来写这条档案需要留意的地方，最长 200 个字符

## 数据存储

所有数据写在 `data/db.json`，结构为 `{ "zones": [...] }`。落盘时先把内容写进同名临时文件，再用改名的方式替换正式文件，写入过程中断也不会把数据文件写坏；文件缺失或内容损坏时会自动回落到初始数据。

## 接口一览

| 方法   | 路径             | 说明                                       |
| ------ | ---------------- | ------------------------------------------ |
| GET    | /api/health      | 服务状态检查                               |
| GET    | /api/zones       | 时区档案清单，支持按是否实行夏令时筛选与关键词搜索 |
| POST   | /api/zones       | 新增时区档案                               |
| GET    | /api/zones/:id   | 查看单条档案                               |
| PATCH  | /api/zones/:id   | 修改档案                                   |
| DELETE | /api/zones/:id   | 删除档案                                   |
| POST   | /api/convert     | 把一个时刻换算到所有已登记的时区           |
| POST   | /api/compare     | 两条档案的时差、夏令时时段与翻日边界对照   |

接口出错时统一返回 `{ "error": { "code": "...", "message": "...", "field": "..." } }`，其中 `field` 用于让页面把问题标到具体的输入项上。
